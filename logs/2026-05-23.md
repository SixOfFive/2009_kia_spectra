# 2026-05-23 — project kickoff

First working session. Established the project scope, architecture, and parts list end-to-end. No hardware yet.

## What was decided today

### Architecture — hybrid ESP32 + Pi Zero 2 W

After exploring three architectures (Pi-only, ESP32-only, hybrid), settled on the hybrid:

- **ESP32**: always-on monitor, real-time RF transmit (CC1101), native CAN bus (TWAI), deep sleep at sub-mA
- **Pi Zero 2 W**: 5" HDMI touchscreen UI, map display, web UI for home monitoring, MQTT publisher
- UART communication between them, ESP32 power-gates the Pi via a logic-level AO3401A P-MOSFET

Rationale: the Pi's WiFi + Linux is what makes home monitoring + maps possible. The ESP32's sub-mA deep sleep is what makes 24/7 battery monitoring possible without draining the car. Each does what it's good at.

### Connection: OBD-II only

Single-point connection through the OBD-II port via a passive Y-splitter:
- Pin 16 (+12V, always-hot) → fused 12V input
- Pin 4/5 (GND)
- Pin 6 (CAN-H), Pin 14 (CAN-L) → SN65HVD230 transceiver

Y-splitter keeps the port available for scan tools. No splicing into the car's harness. 15-minute reversible install.

### Compustar trigger: software-synthesized Keeloq RF

The car has a Compustar 1WSHR-PRO FOB (FCC ID 7087A-R762A433) already installed. The brain is somewhere behind the dash but not visually accessible without disassembly.

Instead of finding the brain's hardwire start input, we'll have the ESP32 transmit a valid Keeloq packet over 433.92 MHz via a CC1101 module. Keeloq has been academically broken; with owner-access to the FOB the practical attacks (chip EEPROM read, manufacturer-key databases) make this very feasible.

Plan:
1. SDR capture of FOB transmissions (RTL-SDR Vomeko dongle on hand)
2. URH analysis to identify framing, serial, function codes
3. Extract device key (Flipper Zero database first, PICkit fallback)
4. MicroPython Keeloq encoder driving CC1101

### Final BOM: $260 CAD for one unit

Detailed in [`docs/BOM.md`](../docs/BOM.md). Single-unit scope so the build is reproducible by anyone reading this repo.

## Compustar discovery photos

Found the brain area while exploring under-dash:
- Identified factory Siemens VDO ECU (D53K label) — not the Compustar
- Spotted the Compustar brain partially obscured behind wiring tape
- Could not photograph the brain's model number despite trying — too tight an angle
- Identified the Compustar windshield antenna (small black 3-LED wedge top-center of windshield)

The model-number obstacle is what pushed us from "find the brain's start trigger wire" to "synthesize the RF packet ourselves." Better outcome anyway — fully reversible install.

## Repository created

- Initial commit: `c630416`
- Pushed to https://github.com/SixOfFive/2009_kia_spectra
- Scaffolding: README, BOM, architecture doc, Compustar research, SDR workflow doc, esp32/ and pi/ stub READMEs

## Process rules established for this project

- Auto commit + push after every change ([memory: vroom-auto-commit])
- Daily log in `logs/YYYY-MM-DD.md` ([memory: vroom-daily-log])
- Single-unit documentation scope for public reproducibility ([memory: vroom-scope])

## Shopping run — late afternoon

Dispatched 4 parallel browser-MCP agents to shop the BOM (build for 2 units) across Amazon.ca and AliExpress, plus 3 follow-up agents for cleanup, SD swap, and tools.

**Final cart subtotals (pre-tax, pre-shipping):**
- Amazon.ca: ~$555.45 CAD (21 items)
- AliExpress: ~$121.60 CAD (12 items)
- **Grand total: ~$677 CAD** for both units + missing tools

**Decisions made during shopping:**
- **Pi Zero 2 W**: $38.35 each on Amazon (market reality vs original $25 target)
- **SD card**: chose SanDisk Ultra 32GB 2-pack at $56.22 instead of Extreme (saved $26)
- **5" display**: Elecrow capacitive touchscreen with integrated USB-A for touch — eliminated the separate USB cable line item
- **ESP32-WROOM-32U**: huge AliExpress win at $3.12 each (vs ~$18 Amazon)
- **Buck converter**: huge AliExpress win at $6.09 each (vs ~$19 Amazon)
- **AO3401A MOSFETs**: 100-pack from AliExpress at $3.59 (way more than needed but cheapest)
- **Wire stripper**: AliExpress generic at $13 (Klein 11057 was $38, too pricey)
- **Hookup wire**: 28 AWG silicone kit instead of 26+22 — note this is on the thin side for the high-current display/Pi paths

**Tools added (user did not have these):**
- Precision wire strippers, liquid flux pen (3-pack), step drill bit (4-32mm), coping saw + blades, precision pliers set

**Items NOT in cart that may be needed later:**
- PICkit programmer ($30) — only if SDR-only Keeloq extraction fails
- Logic analyzer ($15) — only if SPI/CAN debugging gets weird
- Cellular modem + SIM — only for off-network monitoring

**Pending: cart audit before checkout.** Six agents touched the carts; running totals matched expected deltas but a manual count vs the master list is the safe move before placing the order.

## Late evening — orders placed

After several rounds of cart audits, scaling, and cleanup attempts (some agents crashed mid-task, some made silent errors like dropping the ESP32 line entirely), the user took over and placed the orders manually. Final state in carts at order time included:

- Single-unit car-project parts (ESP32, Pi Zero 2 W, Elecrow display, SanDisk Ultra 2-pack, CC1101, ADS1115, SN65HVD230, OBD-II Y-splitter + male pigtail, buck converter, antennas, etc.)
- Extras + future-project additions added during the session: GeeekPi Pi Zero aluminum case, MB102 breadboards 10-pack, additional ESP32-WROOM-32U boards (up to qty 5 for future standalone projects), micro-USB cable 5-pack, AO3401A 100-pack, possibly some ESP32-S3 boards
- Various small tools the user didn't have: precision pliers, coping saw, wire stripper, step drill bit, flux pens

**Lessons learned for future agent-driven shopping:**
- Long-running browser-MCP agents (>5min, >30 tool calls) crashed with socket errors twice in this session
- Cart quantity decrements can silently go to zero (and silently remove the line entirely) if the agent miscounts. Verify after each quantity change.
- Multi-agent shared cart sessions lose state visibility — agents reading the cart at different times see different states. Re-audit is required after every batch of changes.
- Amazon's Asurion Protection Plan modal must be dismissed after every add-to-cart or subsequent adds appear to fail silently
- AliExpress sometimes opens product pages in new tabs when clicking thumbnails — navigate directly to product URLs to stay in the assigned tab
- "Find" tool burns context — prefer `read_page` with `filter="interactive"` and small `max_chars`
- **The "$3 ESP32-WROOM-32U" AliExpress listing the agent kept selecting was a variant-selector trap**: the cheap default variant is just the bundled 2.4 GHz antenna, NOT the board itself. The actual board variant on the same listing is buried deeper at $8-12. The user caught this manually before checkout and picked legitimate alternatives. Documented in [`docs/BOM.md`](../docs/BOM.md#buyer-warning--esp32-wroom-32u-on-aliexpress).

## Late-night code sprint — first 5 modules + tests + dashboard skeleton

Pushed the first code while waiting for parts to ship. Hardware-independent pieces only — everything in this round can be developed and tested on a desktop with plain Python.

| Commit | What |
|---|---|
| `c87e1e7` | `esp32/src/lib/keeloq.py` — KeeLoq block cipher (528 rounds, 32-bit block, 64-bit key) + 4 tests |
| `340c309` | `esp32/src/lib/compustar.py` — HCS66 packet builder + PWM OOK pulse generator + 6 tests |
| `f0ef5a4` | `esp32/src/lib/obd2.py` — 14-PID dict + single-frame ISO 15765-4 query/parse + 9 tests |
| `53cde0e` | `esp32/src/lib/pi_link.py` + `pi/app/comms/esp32_link.py` — line-delimited JSON UART protocol, symmetric on both sides + 6 tests |
| `74a83eb` | `esp32/src/config.py` + `pi/app/config.py` + secrets templates |
| `3cd569e` | `pi/app/display/` — Flask dashboard skeleton: server + dashboard.html + dark-theme CSS + 1Hz polling JS, 6 gauges, 3 buttons |

**Test totals**: 25 passing tests (4 keeloq + 6 compustar + 9 obd2 + 6 pi_link).

**Things confirmed in pure software**:
- KeeLoq cipher matches the canonical fixed point `encrypt(0,0) = 0` and is round-trip consistent — implementation structurally correct.
- HCS packet structure assembles to exactly 66 bits as specified.
- OBD-II PID parsing produces correct values from canned CAN frames (1726 RPM from 0x1AF8, 14.0V from 0x36B0, etc.) matching SAE J1979 formulas.
- Pi ↔ ESP32 schema is symmetric: encoding from either side decodes cleanly on the other.

**Still placeholder** (need SDR captures or actual hardware to confirm):
- Compustar function codes (START / LOCK / UNLOCK / TRUNK 4-bit values)
- TE timing (currently 400µs assumed; HCS chips can be configured 25-1600µs)
- Preamble length and header gap
- Device key, FOB serial, initial counter — these go into `secrets.py` after we read them off the FOB (PICkit) or recover via SDR analysis

**Dashboard preview**: served by `python pi/app/display/server.py` on port 8000. Currently uses mocked STATE — wiring it to the UART listener thread is a Day-3-with-hardware task.

## Next session

When parts arrive:

1. Inventory the boxes — confirm everything that was ordered actually showed up, and identify any DOA modules early
2. Flash MicroPython onto the ESP32
3. Smoke-test each module on breadboard one at a time (ADS1115, CC1101, SN65HVD230, ESP32 WiFi)
4. Begin SDR captures of the FOB at 433.92 MHz
5. Open captures in URH, document framing in `sdr/analysis/framing.md`
6. Wire the dashboard's STATE dict to a real UART listener thread reading from the ESP32

Until then: wait for shipping.
